# Contratos de componentes: PROGRESS (bars / rings / dots).
contracts:
  - id: progress_no_error_color_outside_error
    severity: P2
    description: >
      Widgets de progreso (NMProgressBar/NMRingPulse) con nombre de
      error/warning/danger fuera de un estado de error.
    check: progress_dot_error_color

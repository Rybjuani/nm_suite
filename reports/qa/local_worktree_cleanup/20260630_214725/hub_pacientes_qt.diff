diff --git a/hub/pacientes_qt.py b/hub/pacientes_qt.py
index 4202f39e..b44e44c1 100644
--- a/hub/pacientes_qt.py
+++ b/hub/pacientes_qt.py
@@ -410,9 +410,7 @@ class DetallePacienteView(QWidget):
         if panel_lay is not None:
             while panel_lay.count():
                 panel_lay.takeAt(0)
-            panel_lay.setContentsMargins(
-                V3_SP["lg"], V3_SP["xs"], V3_SP["lg"], V3_SP["sm"]
-            )
+            panel_lay.setContentsMargins(0, 0, 0, 0)
             panel_lay.setSpacing(V3_SP["xs"])
 
         # 2026-06-24: mockup Resumen IA ÔÇö eyebrow UPPERCASE del nombre del
@@ -431,6 +429,7 @@ class DetallePacienteView(QWidget):
             f"color: {v3c('text', self._modo).name()}; background: transparent;"
         )
         panel_lay.addWidget(title_lbl)
+        panel_lay.addSpacing(12)
 
         for paragraph in [p.strip() for p in (text or "").split("\n\n") if p.strip()]:
             lbl = QLabel(paragraph)
@@ -448,7 +447,8 @@ class DetallePacienteView(QWidget):
 
         # 2026-06-24: mockup Resumen IA ÔÇö bot├│n "Cerrar" filled green (primary),
         # no ghost. Antes era texto estilo ghost.
-        dialog.add_footer_button("Cerrar", role="primary", callback=dialog.close)
+        close_btn = dialog.add_footer_button("Cerrar", role="primary", callback=dialog.close)
+        close_btn.setFixedSize(84, 36)
         dialog._panel.setFixedHeight(220)
         dialog.show_centered()
 
